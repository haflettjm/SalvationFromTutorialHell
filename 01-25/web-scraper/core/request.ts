// Response Interface?

// HandleRequest
function HandleRequest(uri: string, headers: string, timeout: number) {
  //Needs to send HTTP Request to URL
  //
  //
  // Handle Redirects, timeouts, retries
  //
  //
  // Return useful Response Data
  return;
}
